# Tools

Scripts and helper tools for working with the WorkPod Pro repo.

## Contents

- `bom-validate.py` — checks parts.csv for missing fields
- `lfs-check.sh` — verifies all large files are tracked by LFS before pushing

## Usage

```bash
python tools/bom-validate.py
bash tools/lfs-check.sh
```
