# Setup Photos

Real WorkPod Pro installs. Exterior and interior.

Committed via Git LFS. Run `git lfs ls-files` to verify before pushing.

Name files: `location-name_exterior.jpg`, `location-name_interior.jpg`
